services:
  backend:
    image: typic-lists-backend
    build: backend
    command: npx node index.js
    volumes:
      - ./backend:/app
      - node_modules_backend:/app/node_modules
      - database:/database
    environment:
      DATABASE_URI: file:/database/typic-lists.db
      PORT: 3000
      HOST: 0.0.0.0
      PASSWORD_HASH: aecd9cd7f5ca210c4dd6a265c4844b4d:a29854c8bdc20fd9bca1884fb6f8c4b5f3dde7fd81884afdc383946f873e28a32b77fff6af3bcba72e69c6a33d5936ecdb78f47a53cc66b83c1439ba96bfe887
      SECRET: secret
    pull_policy: never

  frontend:
    image: typic-lists-frontend
    build: frontend
    command: npx vite --host 0.0.0.0
    ports:
      - 127.0.0.1:5173:5173
    volumes:
      - ./frontend:/app
      - node_modules_frontend:/app/node_modules
    pull_policy: never

volumes:
  node_modules_backend:
  node_modules_frontend:
  database:
